.wrap {
    position: absolute;
    left: 16px;
    bottom: 12px;
    z-index: 20;
}

.button {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    min-height: 40px; /* comfortable touch target on mobile */
    padding: 8px 14px;
    border: 1px solid #8a8a8a;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.94);
    color: #222;
    cursor: pointer;
    font: inherit;
    font-size: 15px;
    font-weight: 600;
}

.button:hover {
    background: #fff;
}

.arrow {
    font-size: 10px;
}

/* Panel opens downward (over the map) — the pill sits at the bottom of the
   header, so an upward panel would clip above the viewport on mobile. */
.panel {
    position: absolute;
    top: calc(100% + 10px);
    left: 0;
    width: 250px;
    padding: 14px 16px 12px;
    background: #fff;
    border: 1px solid #ccc;
    border-radius: 10px;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.3);
}

.label {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    font-size: 13px;
    color: #555;
    margin-bottom: 10px;
}

.value {
    font-size: 16px;
    font-weight: 700;
    color: #222;
}

.slider {
    width: 100%;
    height: 28px; /* fat hit area */
    -webkit-appearance: none;
    appearance: none;
    background: transparent;
    cursor: pointer;
}

.slider:focus {
    outline: none;
}

/* Track */
.slider::-webkit-slider-runnable-track {
    height: 6px;
    border-radius: 3px;
    background: #d8d8d8;
}

.slider::-moz-range-track {
    height: 6px;
    border-radius: 3px;
    background: #d8d8d8;
}

/* Thumb */
.slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 26px;
    height: 26px;
    margin-top: -10px;
    border-radius: 50%;
    background: #0a6e8a;
    border: 3px solid #fff;
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.4);
}

.slider::-moz-range-thumb {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: #0a6e8a;
    border: 3px solid #fff;
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.4);
}

.range_marks {
    display: flex;
    justify-content: space-between;
    font-size: 11px;
    color: #888;
    margin-top: 4px;
}

.reset {
    margin-top: 10px;
    width: 100%;
    min-height: 36px;
    border: 1px solid #ccc;
    border-radius: 8px;
    background: #f4f4f4;
    color: #333;
    cursor: pointer;
    font: inherit;
    font-size: 13px;
}

.reset:hover {
    background: #e8e8e8;
}
